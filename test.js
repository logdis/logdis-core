// --------------------------------------------------------------------------------------------------------------------

// npm
const test = require('tape')

// local
const logdisCore = require('./')

// --------------------------------------------------------------------------------------------------------------------

test('contruct a logdis instance', function(t) {
  t.plan(1)

  t.doesNotThrow(
    function() {
      const logdis = logdisCore()
    },
    'Creating a logdis instance does not throw'
  )
})

// --------------------------------------------------------------------------------------------------------------------
